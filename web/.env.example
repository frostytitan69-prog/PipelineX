# PipelineX Frontend Environment Template

# Production Backend API URL (Render / Production Express API)
# Leave empty in development to fallback to local Vite dev proxy (/api/v1)
VITE_API_URL=https://pipelinex-api.onrender.com
